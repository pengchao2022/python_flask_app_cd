environments:
  production:
    values:
      - global:
          environment: "production"
  staging:
    values:
      - global:
          environment: "staging"
  development:
    values:
      - global:
          environment: "development"
---

releases:
  - name: todo-app
    chart: .
    namespace: argocd
    values:
      - ./helm.gotmpl # values dynamicly 