apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.fullname" . }}-web-service
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
  selector:
    app: {{ include "todo-app.fullname" . }}-web